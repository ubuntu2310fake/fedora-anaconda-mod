# fedora-anaconda-mod
A runtime monkey-patch utility for Fedora's Anaconda WebUI installer. Bypasses NVMe formatting requirements to preserve data, unlocks React-controlled Reformat switches via DOM manipulation, and injects custom post-install data restoration tasks.
